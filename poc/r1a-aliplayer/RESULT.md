# R1a 结论

> 风险项定义见 `docs/04-实施计划.md` §D 的 R1a；被验证的选型见 `docs/DESIGN-CONTRACT.md` §1。
> 本文件记录**已实测的事实**。最终「可行 / 不可行」的勾选与签字由人完成。

- 测试日期：2026-08-14 ~ 2026-08-15
- POC 地址：`https://poc.hqtw.cn/`（服务 `r1a-ali`）

## 验证对象

| 项 | 值 |
| --- | --- |
| 加密 | **阿里云私有加密**（`AliyunVoDEncryption`） |
| 播放器 | Aliplayer 2.25.1 |
| 播放方式 | **VidAuth**（`vid` + `playauth` + `encryptType:1`） |
| 密钥分发 | 点播服务托管，**无自建密钥接口** |

> **选型说明**：契约 §1 原定「HLS 标准加密 + DPlayer + hls.js」。改用阿里云私有加密是**产品侧决策，不是被 R1a 的实测结果逼出来的**——记此一句，以免后续变更评审误读为"原选型已被验证不可行"。原路线的实测数据见文末附录。

转码产物已核实（`GetPlayInfo`）：`Format=m3u8`、`Encrypt=1`、`EncryptType=AliyunVoDEncryption`、单路 1280×720——满足契约 §1 挑选规则 `Format=="m3u8" && Encrypt==true`，且**只存在加密流**（`GetPlayInfo` 不带 `ResultType=Multiple` 时直接返回 `Forbidden.AliyunVoDEncryption`，本身即"确已加密"的证据）。

## 环境

| 机型 | 系统 | 微信 | 内核 | 覆盖 |
| --- | --- | --- | --- | --- |
| Android（24117RK2CC） | Android 16 | 8.0.62.2900 | **XWEB/1460249** | 已测 |
| iOS（iPhone OS 18.7 / 26.3.1） | iOS 18.7、26.3.1 | 8.0.75 | WKWebView | **2026-08-21 已测 ③④；① 仍零覆盖（见下）** |
| Android X5(TBS) | — | — | X5 | 零覆盖 · 可选 |

## ⚠ 2026-08-21 · 口径变更（F-113）与 iOS 实测补记

**需方 2026-08-21 定案 F-113：禁快进不做、拖动放开、允许倍速、观看时长改由前端计时、完播判定第一版延后。**

- **④「前跳能否拦住」不再是产品需求。** 下方 Android 与 iOS 的实测结果**原样保留供回溯**，但**不再构成签字条件**。
- **③（水印）随 F-114 第一版也不做了**（需方 2026-08-21：「水印不做」）。
  **但本文件里 iOS 那条实测结论比以前更值钱**：将来要重启水印时，
  **iOS 微信全屏下 DOM 水印不成立、且自检会报假绿** —— 这两条决定了那时候不是照抄就能用。

### iOS 微信实测（2026-08-21，enc=off 不加密源）

| # | 结果 | 依据 |
| --- | --- | --- |
| ⓪ **不加密播放** | **过 —— 已被 ③④ 的观测隐含证明** | 本轮 iOS 跑的是 `enc=off` **不加密源**。③ 记录了「全屏那 58 秒」的逐秒自检，④ 记录了 **313 次 `Aliplayer.seek` 拦截与 14 次 `seeked` 落点** —— **没有视频在播，这两条一个字都产生不出来**。所以「不加密播放在 iOS 微信里可行」这件事**当时就已经被证明了**，只是表里一直没有这一行，于是看起来还是零覆盖。<br>**补这一行的依据就是 ③④ 的观测本身**，不是另做了一轮测试。<br>**它与 ① 是两条路**：① 说的是**加密**那条路，仍然零覆盖，补测前置是 HTTPS（Service Worker 插件硬性要求），**不挡第一版**（F-114 定案：第一版不加密） |
| ① 加密播放 | **仍是零覆盖 —— 不是「测过不行」** | 8-21 那轮 iOS 报 Aliplayer `code 4010`，当时判成「iOS 不支持私有加密」**是错的，已撤回**：阿里云给 iOS 的方案不走 MSE，走 **Service Worker 插件 `aliplayer-worker-min.js`**（须与播放器同版本、**同源部署**，而 **Service Worker 硬性要求 HTTPS**）。本页**从未接过该插件**、且跑在 `http://` 裸 IP 上 —— **两样都缺，那次 4010 不构成对 iOS 的判定**。补测的前置是一个 HTTPS 环境（域名备案未下来）|
| ② 凭证签发 | **过** | iOS 上 `PlayAuth` 正常签发（长度 2276） |
| ③ 水印全屏 | **不过** | `★ webkitbeginfullscreen` —— iOS 原生全屏接管，**画面上没有水印**（人眼确认，与日志一致）|
| ④ 前跳拦截 | **过**（但已不是需求） | 313 次 `Aliplayer.seek 拦截`；14 次 `seeked` 落点**无一越过 `maxPosition`**；**全屏期间守卫照样工作**（窗口内 331 条 SEEK 事件）|

**⚠ 本轮最值钱的一条：水印自检在 iOS 全屏下会报假绿。**
全屏那 58 秒里自检一路 `connected=true 外层display=block/vis=visible/op=1 内层display=block rect=236x23`，
**而画面上根本没有水印**。自检查的是 DOM 状态，查不到"有没有盖在正在播的视频上"。
**这套自检不能作为水印有效的证据，尤其不能拿去生产环境当监控指标**——在 iOS 上它会一直绿着而实际零保护。
（Android 那轮的 FS 日志其实已经留过话：「只排除了'没渲染'，压不压得住原生视频层要看画面」。iOS 把这句话坐实了。）

**原预测只应验了一半**：`04-实施计划` 曾写「全屏被接管时 DOM 水印**与自定义守卫一并失效**（规则 13 与 14 同时落空）」——
实测**水印落空、守卫没有**。该处已订正。

---

## ⑤ 前端计时（2026-08-22 加，**待需方真机实测**）

**为什么加这一项**：F-113 定案「观看时长改由前端计时、后端照单全收」，
而**前端到底能不能准确计时，从来没有被测过**。
它不成立的话，模块 13 的接口签名（那个累计秒数字段）要重做 ——
**所以先验比先建划算**，而 POC 已经有真机能打开的页面和服务端日志。

**做法：同时报两个数，一次跑完回答两个问题。** 不挑一种实现去测（那只能测出「这一种行不行」）：

| 值 | 怎么算 |
| --- | --- |
| `wallRaw` | 裸墙钟累计：每次 tick 累加 `now - 上次 tick 的 now`，**不封顶** |
| `wallCapped` | 封顶累计：同样算增量，但**每次增量截断到 tick 间隔 + 容差**（tick 1 秒，每次最多计 2 秒）|

两个数的**差**就是节流/冻结造成的虚增 —— 一次跑完同时得到
「**问题有多大**」和「**封顶能不能治**」两个答案。

**三种情况停表**：`paused` / `waiting`(缓冲) / 页面不可见。
**累加的是墙钟不是 `currentTime` 的增量** —— 后者会让倍速把计时一起加速，
正好违反 F-113 规则 4（允许倍速，时长按真实观看时间算）。

**心跳 10 秒一次，报的是【累计值不是增量】**（F-113 定的口径，顺便一起验），
每条带 `currentTime` / `paused` / `visible` / `tickCount` 四个当时状态 ——
没有它们，日志读不出因果。

**服务端一个字都不算**：`/log` 原样落盘，`/admin/logs` 原样显示。本轮要看的就是前端原始报的是什么。

### 已在本地验过的（实测输出见提交说明）

| 项 | 状态 |
| --- | --- |
| 页面能打开、心跳能上报、`/admin/logs` 里看得见那四个字段 | **过**（本地 `PORT=18099`，服务端实收 6 条心跳）|
| 计时算术本身（五个场景）| **过**（`node selftest-timing.js`，跑的是 `index.html` 里那段**真代码**）|
| **真机上的三个场景（Android / iOS 各一遍）** | **待需方测** —— 见 `真机测试须知-计时-2026-08-22.md` |

**⚠ 本地那两条证明不了真机。** 锁屏、后台节流、页面被系统回收，
这三件事**只在真手机上发生**；自检脚本能保证的只是「算术没写错」，
保证不了「手机会不会那样表现」——**而后者正是本轮要测的东西**。

---

## 四项判定（Android XWEB）

| # | 验什么 | 结果 | 依据 |
| --- | --- | --- | --- |
| ① | 加密 HLS 能否播放 | **过** | `player.ready` → `canplay` → 52.23s 播完 `ended`，零错误。阿里云控制台标注的「移动端浏览器支持有限」在该机型上**未成立** |
| ② | 身份能否随密钥请求到达服务端 | **不适用** | 私有加密的密钥由点播服务托管，无自建密钥接口。替代验证项 **PlayAuth 签发：过**（`GetVideoPlayAuth` 正常返回，长度 2288） |
| ③ | 水印 + MutationObserver（含全屏） | **过** | 篡改检测：删节点 / `display:none` 两种手法均立即捕获并暂停锁死。全屏：元素为 `DIV#playerbox.prism-player`，水印在全屏子树内、框 978×440 铺满 977×440 屏幕，且**人眼确认画面上可见** |
| ④ | 前跳能否拦住 | **人工确认通过，Android 侧日志佐证待补**<br>**⚠ 口径已变更（F-113）：不再是产品需求，本行实测结果保留供回溯，不构成签字条件** | 见下。2026-08-21 补测日志：拦截 #1#2#3（46.36s / 47.94s / 48.96s 全被夹回 44.47s），往回拖与已看范围内拖正常放行 |

### ④ 的状态

测试者在 Android XWEB 上口头确认"禁止前跳测了没问题"，但服务端当时三个会话均为 0 拦截记录。复盘出成因是**日志回传的两个缺陷**，已修复：

1. `outbox.splice(0)` 在发送**之前**取走事件——`sendBeacon` 返回 `false` 时无人放回，那批事件永久丢失且静默
2. beacon 的 Blob `type` 用了 `application/json`，不在 CORS 安全列表内，部分 WebView 直接拒收
3. 另加：**`★` 开头的判据立即回传**，不再攒到 3 秒轮询或页面关闭

修复后端到端验证（桌面 Chrome 打线上站，**页面未关闭、未切后台**即已入库）：

```
02:35:20.816 ★ Aliplayer.seek 拦截 #1：目标 40.00s > maxPosition 4.68s → 夹到 maxPosition
02:35:20.823 seeking → 4.68s（允许，maxPosition=4.68s）
02:35:20.862 seeked 落点 4.68s  maxPosition=4.68s
```

`落点 == maxPosition`，符合判过标准。**回传机制已确认可用；Android XWEB 上补跑一次即可闭环**（约一分钟，见下节第 0 项）。

## 结论（二选一，不允许「待观察」）

- [ ] **可行** —— 模块 12/13/14 按本方案推进
- [ ] **不可行** —— 须变更选型，模块 12/13/14 重做，已发起变更评审

**当前状态：结论未定，且已明确还缺什么。** 这与「待观察」不同——后者是含糊过去，前者给出了可执行的收敛路径。

## 签字条件

| 项 | 状态 |
| --- | --- |
| Android XWEB 四项 | ①②③ **已过**；④ 待补日志佐证（第 0 项） |
| **iOS 微信四项** | **零覆盖 —— 必测，不可豁免** |
| Android X5(TBS) | **可选**——与 XWEB 同属 Android 侧，风险远低于 iOS |

**为什么 iOS 必须测、且 Android 上永远替代不了**：§D 明确写 iOS 是「①③④ 最可能挂的地方」，具体是两件 Android 上不会发生的事——

- **全屏被系统播放器接管**（`webkitbeginfullscreen`）：视频交给系统播放器，DOM 水印与自定义守卫在此期间**一并失效**，③④ 同时受影响
- **播放路径可能退化为原生 HLS**：解密与播放交给系统栈，页面侧可观测事件大幅减少

本方案在 iOS 上还多一个未知数：私有加密的解密依赖 Aliplayer SDK 在 **WebKit 内核**下的实现，官方只在控制台标了一句「移动端浏览器支持有限」，未给机型级明细——**这句话在 Android XWEB 上已被证伪，但不能据此推断 iOS。**

## 下一步

### 第 0 项 · Android 补一次 ④ 的日志（约一分钟）

刷新页面使高水位归零 → 只播 5 秒 → 往右拖到 40 秒 → 屏内出现 `★ Aliplayer.seek 拦截` 后，在 `https://poc.hqtw.cn/admin/logs` 确认服务端也有该条。**日志回传已改为判据即刻上传，不必再点「上报日志」或关页面。**

### 第 1 项 · iOS 微信，四件事逐条判

**③④ 必须在全屏下各测一次**——iOS 的风险恰好在那里。

| # | 看什么 | 判过的标准 |
| --- | --- | --- |
| ① | 私有加密视频能否播放 | 播到 `ended`，零错误 |
| ② | PlayAuth 签发与时效 | 服务端 `playauth.jsonl` 有签发记录；凭证过期后拒播 |
| ③ | 水印在**全屏**下是否可见 | `fullscreenchange` 里 `el.contains(wm)` 为真 **且人眼确认画面上可见**。**iOS 全屏若被系统播放器接管（日志出现 `★ webkitbeginfullscreen`），本项直接判不过** |
| ④ | 前跳能否拦住（**含全屏**） | **服务端日志**有 `★ 拦截` 记录，且随后的 `seeked 落点` 等于 `maxPosition` |

iOS 上另需留意页面会打出的两行环境判据：`Hls.isSupported()` 与 `canPlayType(hls)`——若播放退化为原生 HLS，②③④ 的可观测性都会随之改变，须在结论中单独注明。

---

# 给模块 13/14 的实现结论

> 以下是本次踩出来的**实现级结论**，与"能不能播"无关，但**写代码时必踩**。
> 单独列出，是因为它们原本只存在于 POC 的 commit message 里，模块 13/14 的实现者不会去翻 git log。

## ① 全屏水印必须挂在 `fullscreenElement` 的子树内 —— 影响模块 14

原生 Fullscreen API 下，**全屏渲染层只包含进入全屏的那个元素及其后代**。水印若是 video 的兄弟节点，全屏期间画面上必然没有它。

实测：用原生控件的全屏按钮时，全屏元素是 `VIDEO` 本身，水印作为兄弟节点直接消失；改为对**播放器容器**全屏、并把水印挂进容器内部，水印才在全屏子树里。Aliplayer 的情况是——它把 `prism-player` 类直接加在传入的容器元素上、**不另建根节点**，所以水印挂在该容器内即可。

**判据是硬的，不必靠肉眼猜层级**：在 `fullscreenchange` 里判断

```js
el.contains(wm) === false   // → 全屏期间必然不可见，可直接判定
```

**但反向不成立**：`contains` 为真只排除了"没渲染"这一种可能，**压不压得住原生视频层仍需人眼确认**——视频可能被内核提升到独立的原生合成层，DOM 再高的 `z-index` 也压不上去。本次 ③ 正是靠人眼确认才判过。

**备选路径**：若原生全屏在某些内核（尤其 iOS）上保不住水印，可改用**不调用 Fullscreen API 的 CSS 全屏**——纯 CSS 铺满视口，页面仍留在普通文档流里，合成方式与内联一致，水印自然保留。

> 该做法在桌面 Chrome 上验证过：进入后 `document.fullscreenElement` 为 `null`、水印框与视口同尺寸。**真机未测**；Aliplayer 是否提供等价能力需另查，必要时可自行用 CSS 实现。

## ② `max_position` 的维护方式 —— 直接是模块 13 心跳规则的核心数据

**不能用 `timeupdate` 无条件抬高水位。** 拖动进度条时 `timeupdate` 会跟着手指一路触发，**在 `seeking` 判定之前**就把水位抬到目标位置，守卫回头一看"这地方早看过了"就放行。

实测代价：第一版守卫在 XWEB 上触发了 8 次拦截、**7 次最后仍落在前面**，还有一个会话**全程零拦截**（跳过 10 秒毫无阻碍）。

正确做法三条：

1. **只认连续播放造成的推进**——非 `seeking` 状态、且单次增量在合理区间内（实现用 `delta > 0 && delta < 1.5s`；1x 播放下一次 `timeupdate` 约推进 0.05~0.5s，拖拽跳变远大于此）
2. **去掉抑制窗口**——拉回后设一段"不再拦"的冷却期，会正好漏掉拖拽结束那一次 seek
3. **终判放在 `seeked`，不是 `seeking`**——`seeking` 里写回的 `currentTime` 赢不过还在进行中的原生拖拽，会被下一次拖拽立刻覆盖；**只有松手后的最后一次 `seeked` 才是用户真正的落点**，在那里再判一次并兜底拉回

修复后实测：16 次拦截，`seeked 落点`**每次都精确等于** `maxPosition`，全屏下同样拦得住，且后退与已看范围内跳转不误伤。

## ③ Aliplayer 的两处样式陷阱 —— 模块 14 会原样撞上

**其一：它用行内样式管 `video` 尺寸，退出全屏时不还原。** 全屏时写进去的像素高度（实测残留 `height: 775px`）留在行内，退出后 video 溢出容器——现象是"**退出全屏后视频被无限拉长**"。

**其二：它往播放器根节点写 `height:100%`，而父容器高度又依赖它。** 循环解析下计算高度**塌成 `0px`，整个播放区消失**。这是**竞态**——早测一秒还是正常的，晚一点就塌了，很容易漏。

处置：

```css
#playerbox:not(:fullscreen) { height: auto !important; }   /* 让 aspect-ratio 说了算；全屏时交给 UA 的 :fullscreen 规则 */
#playerbox > video { width: 100% !important; height: 100% !important; object-fit: contain; }
```

**不要给 video 加 `position:absolute`。** 试过，画面会缩到左上角一小块（与不做定位的对照页同尺寸比对才发现）。**只压尺寸，不改定位。**

## ④ 播放器必须自绘控件 —— "为什么不用原生 controls"的可验证理由

自绘控件的播放器会把所有 seek 入口收敛到一个函数上，包住它即可拦住全部入口。Aliplayer 的 `seek()` 即是；`references/DPlayer/` 里可以看到同一模式的完整形态——进度条松手、快捷键 ←/→、切集三个入口全部汇聚到 `player.seek(time)`，函数体里只有一行 `this.video.currentTime = time`（`src/js/player.js:194`、`controller.js:117-125`、`hotkey.js:30,38`），且**拖动过程只改进度条视觉、松手才提交一次 seek**。

量化对比：同一段拖拽操作，**原生 `<video controls>` 那条路要拦 14 次才夹住**（浏览器每约 65ms 下发一次 seek，写回不断被覆盖），**自绘控件只需 1 次**。

契约 §1 给出的理由是"保住播放器二开的三项前端能力"——**这一条比它更硬，因为可验证**：自绘控件把 N 次搏斗变成 1 次函数调用拦截。

## ⑤ 埋点日志必须在判据产生的那一刻回传

本次丢过一整轮 ④ 的证据：人在手机上确认拦截生效，服务端却是 0 条。成因有三，模块 13 的心跳上报会撞上同一类问题：

1. **发送前就清空缓冲**——`sendBeacon` 返回 `false`（队列满 / payload 被拒）时无人放回，事件永久丢失且**静默无错**
2. **beacon 的 `Content-Type` 用了 `application/json`**——不在 CORS 安全列表内，部分 WebView 直接拒收
3. **关键事件攒批**——攒到定时轮询或页面关闭再发，用户切走/杀进程就没了

处置：确认送达后才清缓冲（失败留着重试）；beacon 用 `text/plain;charset=UTF-8`（服务端按 JSON 解析 body，不看 `Content-Type`）；**判据级事件即刻单独回传**。

> **没有日志的那一轮不算数。** 这是"人工说测过了"与"可采信的实测"之间的全部区别。

## 方法论：对照实验里不要留下两个真值来源

`x5-video-player-type` 一度被两处写入：播放器配置写 `'h5'`（拉起 X5 自己的全屏播放器）、页面的 `applyVideoAttrs()` 写 `'h5-page'`（同层渲染、页面内联）。**同一个属性被给了两个互斥的值，最终以谁为准取决于写入时序**——而对照实验要的正是可比性，这会让对比失去意义。

处置：**统一到一处写**，并在 1 秒后把**实际生效**的属性读回来打进日志——

```
[ATTR] 1 秒后实际生效的属性 ["webkit-playsinline=true","x5-playsinline=true",
                             "x5-video-player-type=h5-page","x5-video-player-fullscreen=false"]
```

不靠"我设了所以它就是"。这个动作在所有对照实验里都值得保留。

---

## 若最终落成「部分可行」

须落成明确结论 + 处置建议，不能停在描述。**该决定要写进契约 §1，不能留在某个人的记忆里。**

| 受影响的机型/场景 | 事实（不带方案） | 影响面 | 处置决定 | 写进哪份文档的哪一节 |
| --- | --- | --- | --- | --- |
| | | | | |

## 签字

| 角色 | 姓名 | 日期 |
| --- | --- | --- |
| 测试执行 | | |
| 结论确认 | | |

---

# 附录 · 已废弃的对照路线（保留仅供回溯）

> **路线 A：HLS 标准加密 AES-128 + hls.js / DPlayer / xgplayer。**
> 决策已定只走阿里云一条路，**本路线不再重测，其结论不作为 R1a 的判定依据。**
> 此处保留实测数据，仅为日后变更评审回溯时可查。

代码取回方式：

```bash
git checkout poc-r1a-hlsjs-baseline -- poc/r1a-wechat-hls/
```

（标签 `poc-r1a-hlsjs-baseline`，提交 `1af233f`。服务器上的 `r1a-poc` 服务与 `/opt/r1a-poc` 已删除。）

**Android XWEB 上的实测结果**：

| # | 结果 | 依据 |
| --- | --- | --- |
| ① | 过 | `MANIFEST_PARSED` → `KEY_LOADED` → `FRAG_LOADED`，播满全片，零错误；`Hls.isSupported()=true`、`MediaSource=true` |
| ② | 过 | 服务端 3 次收到 `query={"token":"TESTTOKEN123"}`、`hasToken=true`、`referer` 正常——`MtsHlsUriToken` 身份通道在 XWEB 上成立 |
| ③ | 篡改检测过；**全屏项存疑** | 删节点 / `display:none` 两种手法均立即捕获。全屏首测记为「水印消失」，但当时水印仅 190×12、漂移范围错用了外层容器尺寸、诊断边框尚未加入，**疑为误判**；同机同内核下路线 B 全屏水印可见 |
| ④ | 过 | 16 次拦截，`seeked 落点`每次等于 `maxPosition`；**全屏下同样拦住**；后退与已看范围内跳转不误伤 |

**这条路线未被实测推翻**——记录于此，以免日后误读为"标准加密方案已验证不可行"。
