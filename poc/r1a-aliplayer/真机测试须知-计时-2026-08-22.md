# 前端计时 真机测试须知（2026-08-22）

> 给需方看的大白话说明。**这一轮只测一件事：手机上能不能把「学生看了多久」算准。**
> 后端一个字都没改。

---

## 一、为什么要测这个

我们上次定了：**看视频的时长由手机上的网页自己算，算完报给后端，后端照单收下。**

问题是 —— **手机到底能不能算准，我们从来没试过。**

算不准的话，后端那个"累计秒数"的字段就得重做。**先试再建，比先建再返工划算。**

---

## 二、⚠ 先说一件要紧的事：那个端口现在是开着的

去年那轮开的 `8081` 端口，**从那时候一直开到现在，跨了好几轮，一次都没测过**。

那个端口后面挂着一个**发播放凭证的接口，谁都能访问，不需要登录**。
知道地址的人可以拿它给你阿里云账号里的视频换播放凭证。

**所以流程是这样的，顺序别颠倒：**

```
① 开端口  →  ② 测三个场景  →  ③ 立刻关端口  →  ④ 告诉我，我停服务
```

**为什么先关端口再停服务**：端口关着的时候，那个接口在公网上**等于不存在**。
这比"记得停服务"可靠 —— **万一忘了停服务，端口还是关的**。

> **我这边已经把服务停了**（见第六节）。你要测的时候告诉我，我一分钟就能起回来。

---

## 三、要你做的三步

### 第 1 步 · 开端口

阿里云控制台 → **云服务器 ECS** → 左边 **网络与安全 → 安全组**
→ 找到 `47.110.142.8` 这台机器用的安全组 → **配置规则** → **入方向** → **手动添加**：

| 端口范围 | 授权对象 | 说明 |
| --- | --- | --- |
| `8081/8081` | `0.0.0.0/0` | 计时测试临时，测完删掉 |

### 第 2 步 · 告诉我一声

我把服务起回来，顺手确认一遍页面能开，然后回你一个可以直接点的链接。

### 第 3 步 · 手机上打开这个地址

```
http://47.110.142.8:8081/?vid=你的VideoId
```

VideoId 在哪找：控制台 → 视频点播 → **媒资库 → 音/视频** → 找一个状态**「正常」**的视频，
点进去，基本信息里那串数字就是。**给我，我可以先替你确认它能用。**

---

## 四、要跑的三个场景

**Android 手机和 iPhone 各跑一遍。** 这三个场景在两个系统上的表现不一样，
只测一台等于只测了一半。

页面上有两个数字，就在视频下面那一行：

- **裸计时** —— 老老实实按时间加，不做任何修正
- **封顶计时** —— 也按时间加，但**每次最多加 2 秒**

**两个数一起看**：它们差多少，就是手机"偷偷多算"了多少；
封顶那个准不准，就说明这个补救办法管不管用。**一次跑完，两个问题一起有答案。**

> 每个场景开始前，点一下页面上的 **【计时清零】** 按钮。

### 场景一 · 正常播放

**怎么做**：点播放，**连续放 5 分钟，中间什么都别碰。**

**应该看到**：两个数字都是 **300 秒左右**。

| 看到什么 | 说明什么 |
| --- | --- |
| 都 ≈ 300 | 正常 ✅ |
| 差得多（比如 260 或 340）| **计时本身就不准**，这个方案要重新想 |

### 场景二 · 切后台

**怎么做**：
1. 播 **1 分钟**
2. 切出去到微信聊天界面，**停 30 秒**
3. 切回来，**再播 1 分钟**

**应该看到**：两个数字都是 **120 秒左右**。**中间那 30 秒不该算进去。**

| 看到什么 | 说明什么 |
| --- | --- |
| ≈ 120 | 正常 ✅ |
| ≈ 150 | **切后台没停表**，那 30 秒被算进去了 |

### 场景三 · 锁屏（**最容易出问题的，重点看这个**）

**怎么做**：
1. 播 **1 分钟**
2. **按电源键锁屏，等 2 分钟**
3. 解锁回来看那两个数字

**应该看到**：还是 **60 秒左右**。

| 看到什么 | 说明什么 | 我们怎么办 |
| --- | --- | --- |
| 裸计时变成 180、**封顶计时还是 60** | **封顶这个办法有效** | 就按封顶那个实现 ✅ |
| **两个都变成 180** | 停表根本没生效 | 我要去查手机发的那几个事件 |
| 数字**归零或者乱跳** | 页面被系统回收了 | 那是另一类问题，得换个做法 |

---

## 五、测完怎么办

1. **把三个场景、两台手机的数字记下来发我**（拍页面截图最省事）。
   哪怕数字很难看也照发 —— **难看的数字才是有用的数字**，我要的就是真实表现。
2. **回去把安全组那条 8081 规则删掉。**
3. 告诉我一声，我停服务。

> 我这边能看到手机报上来的全部原始数据（`http://47.110.142.8:8081/admin/logs`），
> 所以你不用记细节，**把两个数字发我就行**。

---

## 六、这一轮我改了什么

| 改了什么 | 说明 |
| --- | --- |
| POC 页面 `index.html` | 加了计时和心跳（每 10 秒往服务器报一次当前累计值） |
| 新增 `selftest-timing.js` | 电脑上先把**算术**验一遍的小脚本，五个场景全过 |
| `RESULT.md` | 补了两段记录 |
| **后端** | **一个字都没改** |
| **服务器上的正式服务** | **没碰**，POC 是独立端口独立服务 |
| POC 服务 `r1a-poc` | **已停**（见第二节） |

---

## 七、两件我先说清楚、免得测出来吓一跳

**① 手机上那个数字不会一秒一秒跳得特别准。**
差个一两秒是正常的，我们关心的是**差 30 秒、差 2 分钟**这种量级。

**② 电脑上我已经验过的，和手机上要验的，不是一回事。**
我在电脑上验的是**算术没写错**（加减法对不对）。
**锁屏、切后台、页面被系统回收 —— 这三件事只在真手机上发生**，
电脑上再怎么验也验不出来。**所以这一轮非得你用真机跑一遍不可。**
