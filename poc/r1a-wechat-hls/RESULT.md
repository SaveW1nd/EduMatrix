# R1a 结论

> **模板，待真机测完由人填写。** 判定口径见同目录 `README.md` §三。
> 风险项定义见 `docs/04-实施计划.md` §D 的 R1a；被验证的选型见 `docs/DESIGN-CONTRACT.md` §1。

- 测试日期：
- 测试人：
- POC 部署地址：
- `make-hls.sh` 用的 BASE_URL / TOKEN：

## 环境

| 机型 | 系统版本 | 微信版本 | 内核（X5/XWEB/WKWebView） |
| --- | --- | --- | --- |
| iOS 微信 | | | |
| Android 微信（X5） | | | |
| Android 微信（XWEB） | | | |

> 内核以页面 `ENV` 区打出的 `xweb` / `tbs` 字段或 UA 为准，不要凭机型猜。

## 四项判定

**判定以第 1 轮 `player=raw`（裸 hls.js）为准。** 每格填 `过` / `不过`，并在下方「逐条实测记录」里写清依据。

| # | 验什么 | iOS 微信 | Android X5 | Android XWEB | 截图 |
| --- | --- | --- | --- | --- | --- |
| ① | AES-128 加密 HLS 能否播放 | | | | |
| ② | key 请求到达服务端，且 URL 参数完整 | | | | |
| ③ | 水印盖在画面之上（含全屏） | | | | |
| ④ | 向前拖拽能被拦住（含全屏） | | | | |

## 逐条实测记录

### ① 加密 HLS 播放

| 机型 | 实际走的引擎（hls.js / 原生） | `Hls.isSupported()` | `MediaSource` / `ManagedMediaSource` | 关键日志行 | 结论 |
| --- | --- | --- | --- | --- | --- |
| iOS | | | | | |
| X5 | | | | | |
| XWEB | | | | | |

### ② key 请求与 URL 参数

| 机型 | 服务端是否收到 `/key` | 收到的 `query` 原文 | `token` 是否完整 | 请求头里有无异常改写（Referer/Origin/UA） | 结论 |
| --- | --- | --- | --- | --- | --- |
| iOS | | | | | |
| X5 | | | | | |
| XWEB | | | | | |

> 若 iOS 走的是原生 HLS 路径，此格必须单独注明——原生路径下密钥请求由系统发起，
> 页面侧观测不到，判据只有服务端 `/key` 日志。

### ③ 水印

| 机型 | 内联播放时可见 | 全屏时可见 | 全屏由谁接管（页面 / X5 / iOS 系统播放器） | 删除水印后是否立即暂停 | 结论 |
| --- | --- | --- | --- | --- | --- |
| iOS | | | | | |
| X5 | | | | | |
| XWEB | | | | | |

### ④ 禁止向前拖拽

| 机型 | 内联时拖拽被拦 | 全屏时拖拽被拦 | 「前跳 +30s」后的实际落点 | 结论 |
| --- | --- | --- | --- | --- |
| iOS | | | | |
| X5 | | | | |
| XWEB | | | | |

### 对照轮次（用来区分"不行"与"某个属性没加所以不行"）

| 轮次 | URL | iOS | X5 | XWEB | 与第 1 轮的差异 |
| --- | --- | --- | --- | --- | --- |
| 2 关同层渲染属性 | `?x5=off&inline=off` | | | | |
| 3 叠 DPlayer | `?player=dplayer` | | | | |
| 4 强制 hls.js（仅 iOS） | `?engine=hlsjs` | | — | — | |
| 5 强制原生（仅 iOS） | `?engine=native` | | — | — | |

## 结论（二选一，不允许「待观察」）

- [ ] **可行** —— 契约 §1「HLS 标准加密 + DPlayer + hls.js」成立，模块 12/13/14 按原设计推进
- [ ] **不可行** —— 须回到阿里云私有加密 + 阿里云播放器，模块 12/13/14 重做，已发起变更评审

## 若「部分可行」

也要落成明确结论 + 处置建议，不能停在描述。例如：

- 「iOS 限制为内联播放、不提供全屏按钮」
- 「iOS 单独换播放方案」

**该决定要写进契约 §1，不能留在某个人的记忆里。**

| 受影响的机型/场景 | 事实（不带方案） | 影响面 | 处置决定 | 写进哪份文档的哪一节 |
| --- | --- | --- | --- | --- |
| | | | | |

## 影响面回填（对照 04 §D 的 R1a）

只在对应项**不成立**时填。

| 不成立项 | 04 §D 写明的影响面 | 本次是否触发 | 已发起的动作 |
| --- | --- | --- | --- |
| ① | 契约 §1「HLS 标准加密 + DPlayer + hls.js」整条选型失效，回到阿里云私有加密 + 阿里云播放器，模块 12/13/14 全部重做 | | |
| ② | `MtsHlsUriToken` 身份通道失效，密钥接口无法鉴权，契约 §2.5「撤销级联回收」的落地点整个消失 | | |
| ③④ | PRD F2-5 / F2-6 两条一级卖点降级为"仅 Android 有效"，须与需求方重新约定验收口径 | | |

## 签字

| 角色 | 姓名 | 日期 |
| --- | --- | --- |
| 测试执行 | | |
| 结论确认 | | |
