-- ============================================================================
-- F-72 定案：题库分类的增删改【仅 org_admin】，撤销 teacher 的三行绑定
--
-- 【它修的是一个「以为存在、实际从未生效的保障」——本项目第七次】
-- 03-04 §1.2/§1.3/§1.4 与 PRD F3-1 规则 8 都写「仅 org_admin」，各自附了论证：
--   qb_category 全租户共享、不带 owner_node_id、不进 org_resource_grant，
--   落在「数据范围由树决定」这条规则的【管辖之外】——
--   树能限制谁看到哪些题目，【限制不了谁改分类树】。
--   任一教师改个分类名或删个空分类，全机构题库目录立刻跟着变。
-- 而 DESIGN-CONTRACT §10 附表 A（自称权威）与 V202608140000 初始化脚本
-- 【一致地】把 question:category:add / edit / remove 绑给了 teacher。
-- 也就是说：只写 @SaCheckPermission("question:category:add") 的话，
-- 【教师会照样通过】——那道门从来没有关上过，而它不会报错。
--
-- 【为什么改数据而不是在代码里加角色门】（F-72 定案理由，逐条）
--   ① 角色门会是全库唯一一处不同构的判定，多一个「会配错的地方」——
--      而这个项目已经在「两份同源实现」上栽过；
--   ② 附表 A 自称与本脚本同源，现在它俩是【一致地错】。
--      改脚本 + 改附表，同源关系保住（契约 §10 已同步订正）；
--   ③ 论证只在分册与 PRD 那一侧，附表 A 那边一句论证都没有。
--
-- 【为什么是 DELETE 而不是把菜单行 visible 置 0】
--   sys_menu 行要留着——按钮本身没错，错的是绑给了谁。删的是 sys_role_menu 的绑定。
--
-- 【可重复执行】：按 (role_id, menu_id) 精确删，删过再跑影响 0 行。
-- ============================================================================

DELETE FROM `sys_role_menu`
 WHERE `role_id` = 1953827104412590203      -- teacher
   AND `menu_id` IN (1949000000000400105,   -- question:category:add   新建题库分类
                     1949000000000400106,   -- question:category:edit  修改题库分类
                     1949000000000400107);  -- question:category:remove 删除题库分类
