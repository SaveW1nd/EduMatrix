-- ============================================================================
-- V202608160100__fix_anonymized_at_comment.sql
-- 订正 org_student.anonymized_at 的列注释：「接口 25 返回 400」→「返回 10209」
--
-- 【七处对一处】全库写 10209 的有七处：
--     docs/01-PRD-产品需求文档.md:1437   F7-3 第四条验收标准「Then 返回 10209」
--     docs/04-实施计划.md:538            模块 07 必读文档表（03-02 §6.10 那一行）
--     docs/04-实施计划.md:554            模块 07 规则 12
--     docs/03-API接口文档/00-通用约定.md:367  §9.2 错误码总表
--     docs/03-API接口文档/02-组织机构.md      §6.10 正文
--     docs/03-API接口文档/02-组织机构.md      §11.3 本模块错误码表
--     backend/.../common/errorcode/ErrorCode.java:110  STUDENT_ALREADY_ANONYMIZED(10209, …)
--   写 400 的只有 V202608120000__baseline.sql 第 417 行的这条列注释。
--
-- 【为什么取 10209 而不是 400】00-通用约定 §9.2 那一格专门论证过：
--   「这不是参数问题而是对象状态问题（参数完全合法），故用业务码而非 400，
--     与 10204『学生未处于归档状态，不可恢复』同形」。
--   而 10209 早在模块 01 就进了 ErrorCode —— 照 400 实现等于让一个已登记的错误码
--   永不触发，正是 F-22 ② 说的「永不触发的码是历史包袱的来源」。
--
-- 【为什么走增量脚本而不是直接改基线】契约 §7.3：V202608120000 是 Flyway 初始基线，
--   此后只走增量脚本，否则新老环境分叉（已建库的环境不会重跑基线，
--   直接编辑基线文件还会让 Flyway 的 checksum 校验失败、整个迁移链卡住）。
--   注释也是 schema 的一部分，同样适用。
--
-- 【只改注释，不改类型与约束】DATETIME NULL DEFAULT NULL 原样保留。
--   MySQL 的 MODIFY COLUMN 必须重述完整定义，漏写 NULL 会把它变成 NOT NULL——
--   而这一列「NULL = 未脱敏」是脱敏任务三个与门里的第三个，改了会让全表被判为已脱敏。
--
-- 登记见 04-实施计划.md §E 的 F-28（已修正）。
-- ============================================================================

ALTER TABLE `org_student`
  MODIFY COLUMN `anonymized_at` DATETIME NULL DEFAULT NULL
  COMMENT '脱敏完成时间，NULL=未脱敏。脱敏不可逆：guardian_phone 与 sys_user.phone 被覆写为掩码，原值不再存于任何地方，故本列非空的学员不可归档恢复（接口 25 返回 10209 学员已脱敏，不可恢复——参数合法、对象状态不允许，故用业务码而非 400，与 10204 同形，见 00-通用约定 §9.2）。定时任务扫 archive_reason=2 且 archive_time 早于 30 日前且本列为 NULL';
